/**
 * useCelebration — re-export from CelebrationContext for convenience.
 * Usage: const { triggerCelebration } = useCelebration();
 */
export { useCelebration } from "@/context/CelebrationContext";
export type { CelebrationType, CelebrationData } from "@/context/CelebrationContext";
